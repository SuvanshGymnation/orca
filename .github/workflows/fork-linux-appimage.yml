# Fork-only: build the Linux AppImage on a GitHub runner and publish it as a
# release asset. The VPS never builds — a clean build OOMed at 6 GB there while
# the agent fleet needs 5.2 GB of the box's 16 GB.
name: fork-linux-appimage

on:
  # A branch push must build. workflow_dispatch alone would not have run here:
  # GitHub only offers it for workflows present on the default branch, so this
  # file on a feature branch would have been a pipeline that never triggers.
  push:
    branches: ['suvansh/**']
    tags: ['v*-suvansh.*']
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to publish (e.g. v1.4.200-suvansh.1)
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Required by verify:computer-native inside build:release. Upstream
      # installs these in the same job (release-cut.yml, linux matrix leg).
      - name: Install Linux computer-use provider dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-gi gir1.2-atspi-2.0 at-spi2-core xclip xdotool rpm

      # The repo's own gate is `lint`, which is wider than typecheck: it caught
      # four localization keys missing from en.json that typecheck could not see.
      - name: Lint
        run: pnpm run lint

      # build:release does not typecheck, so packaging can succeed on code that
      # does not compile. The VPS cannot run this - tsc OOMs there at 4 GB.
      - name: Typecheck
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        run: pnpm run typecheck

      # The step run 34801248574 was missing. build:release produces out/relay
      # (build:relay) and the packaged CLI (build:cli); without it electron-builder
      # reached afterPack and failed on "file source doesn't exist .../out/relay"
      # and "Missing unpacked CLI package boundary" in stampPackagedCliVersion.
      # Upstream runs exactly this before its release_command.
      - name: Build app
        run: pnpm build:release

      # Matches upstream release-cut.yml:1164 rather than inventing a target.
      - name: Build AppImage
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: |
          node config/scripts/ensure-native-runtime.mjs --runtime=electron
          pnpm exec electron-builder \
            --config config/electron-builder.config.cjs \
            --linux AppImage --x64 --publish never

      # The VPS verifies by architecture, so fail here if it is wrong rather
      # than shipping an artifact Step 12's `file` check will reject.
      - name: Verify artifact shape
        run: |
          set -euo pipefail
          APP=$(find dist -maxdepth 1 -name '*.AppImage' -print -quit)
          [ -n "$APP" ] || { echo "FAIL: no AppImage produced"; exit 1; }
          LC_ALL=C file "$APP" | grep -q 'ELF .* executable'
          LC_ALL=C file "$APP" | grep -qF 'x86-64'
          sha256sum "$APP" | tee "$APP.sha256"
          echo "built: $APP"

      # Always keep the artifact, so a branch build proves itself without
      # publishing a release. The release step below only runs for real tags.
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: orca-linux-appimage
          path: |
            dist/*.AppImage
            dist/*.AppImage.sha256
          if-no-files-found: error
          retention-days: 14

      - name: Publish release asset
        if: startsWith(github.ref, 'refs/tags/') || inputs.tag != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: |
            dist/*.AppImage
            dist/*.AppImage.sha256
